# POWER SCALE v14.7

- Reestrutura a troca de moeda em tempo real: queries React Query são reexecutadas usando respostas HTTP preservadas em memória, sem chamadas ao backend.
- Adiciona recarga local, também sem rede, para telas que mantêm dados em `useState` e não dependem do React Query: Gestão de Produtos, Resultados Empresa, Receitas e Despesas e Garimpagem.
- Amplia a conversão para métricas monetárias de campanha e segmentos: custo, CPC médio, CPM médio, custo médio, CPA/CPA alvo, CPC máximo, orçamento, valor de conversão, checkout value e demais valores monetários.
- `/google-ads/segments` passa a devolver buckets monetários por moeda e aliases `avg_cpc`/`avg_cpm`.
- `/google-ads/metrics/funnel` passa a devolver `money_by_currency` e converte apenas cards monetários (não converte contagens).
- Adiciona o endpoint `/google-ads/campaigns/:id/daily-metrics` com `currency_code` para o detalhe diário de campanha.
- Dashboard passa a devolver totais e campanhas com contexto de moeda para conversão consistente.
- Mantém `Original` sem conversão e preserva a moeda de origem de cada conjunto de dados.
- Inclui testes locais de transformação, replay React Query e recarga manual sem rede.
