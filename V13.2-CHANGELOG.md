# POWER SCALE v13.2

- Corrige propagação de `currency_code` da conta Google Ads sem inventar BRL no modo Original.
- Usa `payload.currency` como fallback legítimo da própria importação Google Ads quando a coluna ainda estiver nula.
- AwesomeAPI: consulta direta pelo navegador e fallback pelo backend; suporte a par inverso.
- Resposta de câmbio passa a informar pares indisponíveis em vez de retornar silenciosamente vazio.
- Troca de moeda e taxa personalizada sem `location.reload()`: atualização em tempo real via refetch das consultas ativas do React Query.
- Mantém `Original` como comportamento padrão e não converte dados persistidos.
